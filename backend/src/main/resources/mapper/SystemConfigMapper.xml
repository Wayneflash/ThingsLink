<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iot.platform.mapper.SystemConfigMapper">
    
    <!-- 根据配置键查询配置 -->
    <select id="getByConfigKey" resultType="com.iot.platform.entity.SystemConfig">
        SELECT id, config_key, config_value, description, create_time, update_time
        FROM system_config
        WHERE config_key = #{configKey}
    </select>
    
    <!-- 根据配置键更新配置值 -->
    <update id="updateByConfigKey">
        UPDATE system_config
        SET config_value = #{configValue},
            update_time = NOW()
        WHERE config_key = #{configKey}
    </update>
</mapper>
