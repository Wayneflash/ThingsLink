# 生产环境配置（内网）

# 数据库配置
db:
  url: jdbc:mysql://127.0.0.1:3306/iot_platform?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
  username: root
  password: root123456

# Redis 配置
redis:
  host: 127.0.0.1
  port: 16379

# MQTT 配置
mqtt:
  broker-url: tcp://127.0.0.1:1883
