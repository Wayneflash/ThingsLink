# 🚀 物联网平台前端快速使用指南

## 📦 项目启动

### 1. 安装依赖
```bash
cd frontend
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```

### 3. 访问应用
浏览器打开: `http://localhost:5173`

---

## 🔐 登录测试

### 测试账号
```
用户名: admin
密码: admin123
```

### 登录流程
1. 输入用户名和密码
2. 可勾选"记住密码"（下次自动填充）
3. 点击"登录"按钮
4. 登录成功后自动跳转到设备概览页面

---

## 📄 已开发页面一览

### ✅ 核心功能页面（已完成）

| 页面名称 | 路由路径 | 功能描述 | 状态 |
|---------|---------|---------|------|
| 登录页面 | `/login` | 用户登录认证 | ✅ 完成 |
| 设备概览 | `/overview` | 统计卡片 + 图表展示 | ✅ 完成 |
| 设备管理 | `/devices` | 设备列表、增删改查 | ✅ 完成 |
| 用户管理 | `/users` | 用户列表、权限管理 | ✅ 完成 |
| 角色管理 | `/roles` | 角色配置、权限分配 | ✅ 完成 |

### 🔨 待完善页面

| 页面名称 | 路由路径 | 功能描述 | 状态 |
|---------|---------|---------|------|
| 报警日志 | `/alarms` | 告警列表、处理记录 | 🔨 待完善 |
| 产品列表 | `/products/list` | 产品管理、属性配置 | 🔨 待完善 |
| 产品属性 | `/products/attributes` | 产品属性定义 | 🔨 待完善 |
| 设备分组 | `/groups` | 树形分组管理 | 🔨 待完善 |
| 数据查询 | `/data-query` | 历史数据查询 | 🔨 待完善 |

---

## 🎨 页面功能详解

### 1. 设备概览页面 (`/overview`)

#### 功能特性
- **统计卡片**
  - 设备总数：156
  - 在线设备：142
  - 告警数量：3
  - 今日数据量：12.5K

- **实时数据趋势图**（ECharts 折线图）
  - 显示最近24小时的数据上报量
  - 平滑曲线 + 面积填充
  - 悬停显示详细数据

- **设备分组统计图**（ECharts 饼图）
  - 环形图展示各分组设备分布
  - 交互式图例
  - 支持点击查看详情

- **近期告警列表**
  - 显示最新3条告警信息
  - 告警类型、内容、时间、状态
  - 点击"查看全部"跳转告警页面

#### 操作说明
- 卡片支持悬停动画
- 图表随窗口大小自适应
- 点击"刷新"按钮重新加载图表数据

---

### 2. 设备管理页面 (`/devices`)

#### 功能特性
- **搜索和筛选**
  - 搜索框：支持设备名称/编码搜索
  - 状态筛选：全部/在线/离线
  - 分组筛选：按所属分组过滤

- **设备列表**
  - 表格展示：ID、名称、编码、产品类型、分组、状态、最后上线时间
  - 状态标签：在线（绿色）/ 离线（灰色）
  - 分页功能：支持每页10/20/50/100条

- **设备操作**
  - 添加设备：打开对话框填写设备信息
  - 编辑设备：修改设备信息（编码不可改）
  - 删除设备：删除确认对话框
  - 查看详情：跳转设备详情页（待开发）

#### 操作说明
1. **添加设备**
   - 点击"添加设备"按钮
   - 填写：设备名称、设备编码、产品类型、所属分组、备注
   - 点击"确定"保存

2. **编辑设备**
   - 点击设备行的"编辑"按钮
   - 修改设备信息（编码不可修改）
   - 点击"确定"保存

3. **删除设备**
   - 点击设备行的"删除"按钮
   - 确认删除操作

4. **搜索/筛选**
   - 输入关键词后自动搜索
   - 选择筛选条件后立即生效

---

### 3. 用户管理页面 (`/users`)

#### 功能特性
- **搜索和筛选**
  - 搜索框：用户名/姓名搜索
  - 角色筛选：按角色过滤用户
  - 状态筛选：启用/禁用

- **用户列表**
  - 显示：ID、用户名、姓名、邮箱、手机号、角色、状态、创建时间
  - 状态开关：直接切换启用/禁用
  - 角色标签：显示用户角色

- **用户操作**
  - 添加用户：创建新用户
  - 编辑用户：修改用户信息
  - 删除用户：删除非超管用户
  - 重置密码：重置为默认密码（123456）
  - 启用/禁用：切换用户状态

#### 操作说明
1. **添加用户**
   - 点击"添加用户"按钮
   - 填写：用户名、姓名、密码、邮箱、手机号、角色
   - 表单验证：
     - 用户名：3-20个字符
     - 密码：至少6个字符
     - 邮箱：正确的邮箱格式
     - 手机号：11位手机号
   - 点击"确定"保存

2. **编辑用户**
   - 点击用户行的"编辑"按钮
   - 修改用户信息（用户名不可修改）
   - 点击"确定"保存

3. **重置密码**
   - 点击用户行的"重置密码"按钮
   - 确认操作后，密码重置为 `123456`

4. **启用/禁用用户**
   - 直接切换状态开关
   - 立即生效

---

### 4. 角色管理页面 (`/roles`)

#### 功能特性
- **角色列表**
  - 显示：ID、角色名称、角色编码、描述、用户数、状态、创建时间
  - 超级管理员标识
  - 用户数统计

- **角色操作**
  - 添加角色：创建新角色
  - 编辑角色：修改角色信息（超管不可编辑）
  - 删除角色：删除空角色（有用户不可删除）
  - 权限配置：树形结构配置权限

- **权限配置**
  - 菜单权限：数据监控、设备分组、设备管理、产品管理等
  - 按钮权限：查看、创建、编辑、删除
  - 树形选择：支持全选、半选、单选

#### 操作说明
1. **添加角色**
   - 点击"添加角色"按钮
   - 填写：角色名称、角色编码、描述
   - 角色编码：只能包含小写字母和下划线（如：device_admin）
   - 点击"确定"保存

2. **编辑角色**
   - 点击角色行的"编辑"按钮
   - 修改角色信息（编码不可修改）
   - 点击"确定"保存

3. **权限配置**
   - 点击角色行的"权限配置"按钮
   - 勾选菜单权限和按钮权限
   - 点击"保存权限"

4. **删除角色**
   - 点击角色行的"删除"按钮
   - 只能删除没有用户的角色
   - 超级管理员不可删除

---

## 🎯 核心特性

### 1. 动态菜单
- 根据登录接口返回的 `menus` 字段动态生成侧边栏
- 支持一级菜单和二级子菜单
- 自动高亮当前页面菜单项
- Element Plus 图标动态加载

### 2. 权限控制
- 登录后保存用户信息、token、菜单数据到 LocalStorage
- 路由守卫：未登录自动跳转登录页
- 菜单权限：根据用户权限显示菜单
- 按钮权限：（预留，可扩展）

### 3. 表单验证
- 必填项验证
- 格式验证（邮箱、手机号、角色编码）
- 长度验证
- 自定义验证规则

### 4. 数据可视化
- ECharts 折线图（实时数据趋势）
- ECharts 饼图（设备分组统计）
- 图表自适应窗口大小
- 平滑动画效果

### 5. 响应式布局
- 桌面端：侧边栏 + 内容区
- 移动端：响应式适配（可扩展为抽屉式菜单）
- 表格横向滚动
- 卡片自适应

---

## 🔧 API 接口对接

### 当前状态
- ✅ 登录接口已对接（`/auth/login`）
- ⏳ 其他接口使用模拟数据

### 对接步骤

#### 1. 创建 API 文件
在 `src/api/` 目录下创建对应的 API 文件：

**设备管理 API** (`src/api/device.js`)
```javascript
import request from '@/utils/request'

export const deviceApi = {
  // 设备列表
  list: (params) => request.post('/devices/list', params),
  
  // 添加设备
  create: (data) => request.post('/devices/create', data),
  
  // 编辑设备
  update: (data) => request.post('/devices/update', data),
  
  // 删除设备
  delete: (id) => request.post('/devices/delete', { id })
}
```

**用户管理 API** (`src/api/user.js`)
```javascript
import request from '@/utils/request'

export const userApi = {
  list: (params) => request.post('/users/list', params),
  create: (data) => request.post('/users/create', data),
  update: (data) => request.post('/users/update', data),
  delete: (id) => request.post('/users/delete', { id }),
  updateStatus: (id, status) => request.post('/users/status', { id, status })
}
```

#### 2. 在页面中引入 API
```javascript
import { deviceApi } from '@/api/device'

// 获取设备列表
const loadDevices = async () => {
  loading.value = true
  try {
    const result = await deviceApi.list({
      page: currentPage.value,
      pageSize: pageSize.value,
      searchQuery: searchQuery.value,
      filterStatus: filterStatus.value
    })
    
    if (result.code === 200) {
      devices.value = result.data.list
      totalDevices.value = result.data.total
    }
  } catch (error) {
    ElMessage.error('加载失败')
  } finally {
    loading.value = false
  }
}
```

---

## 📊 数据格式说明

### 登录接口响应
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "xxx",
    "user": {
      "id": 1,
      "username": "admin",
      "realName": "超级管理员"
    },
    "menus": [
      {
        "code": "overview",
        "name": "设备概览",
        "path": "/overview",
        "icon": "DataAnalysis",
        "sort": 1,
        "children": null
      }
    ]
  }
}
```

### 设备列表响应
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 156,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "id": 1,
        "name": "温度传感器-01",
        "code": "TEMP001",
        "product": "环境监测传感器",
        "group": "生产车间",
        "status": "online",
        "lastOnline": "2025-12-28 16:45:23"
      }
    ]
  }
}
```

---

## 🐛 常见问题

### 1. 登录后页面空白
**原因**：未正确返回菜单数据  
**解决**：检查登录接口是否返回 `menus` 字段

### 2. 图表不显示
**原因**：图表容器高度为0  
**解决**：确保图表容器有明确的高度（如 `height: 300px`）

### 3. 菜单图标不显示
**原因**：图标组件未注册  
**解决**：在 `MainLayout.vue` 中导入对应的 Element Plus 图标

### 4. 表单验证不生效
**原因**：未调用 `validate` 方法  
**解决**：在保存前调用 `formRef.value.validate()`

---

## 🚀 下一步开发建议

### 1. 完善待开发页面
- 设备分组页面（树形结构）
- 数据查询页面（图表展示）
- 报警日志页面（完善功能）
- 设备详情页面（实时数据）

### 2. 功能增强
- [ ] 实时数据推送（WebSocket）
- [ ] 文件上传（设备批量导入）
- [ ] 数据导出（Excel）
- [ ] 图片查看器
- [ ] 打印功能

### 3. 用户体验优化
- [ ] 骨架屏加载
- [ ] 操作引导
- [ ] 暗黑模式
- [ ] 多语言支持

### 4. 性能优化
- [ ] 虚拟滚动（大数据表格）
- [ ] 图片懒加载
- [ ] 路由懒加载优化
- [ ] 缓存策略

---

## 📚 技术栈文档

- [Vue 3 官方文档](https://cn.vuejs.org/)
- [Element Plus 组件库](https://element-plus.org/zh-CN/)
- [ECharts 图表库](https://echarts.apache.org/zh/index.html)
- [Tailwind CSS](https://tailwindcss.com/docs)

---

## ✅ 开发检查清单

- [x] 登录页面
- [x] 动态菜单
- [x] 路由守卫
- [x] 设备概览（图表）
- [x] 设备管理（CRUD）
- [x] 用户管理（CRUD）
- [x] 角色管理（权限配置）
- [ ] 设备分组
- [ ] 数据查询
- [ ] 报警日志
- [ ] 产品管理
- [ ] 设备详情

---

**祝开发顺利！🎉**
